# 剖析CSS的規則
做得好！現在`main.css`以連結到`index.html`

在我們撰寫自己的CSS之前，讓我們來看CSS如何運作：

1. rule：一個CSS的指令列表定義如何套用樣式給一堆HTML元素

2. selector(選擇器)：精確的定義哪個HTML如何套用樣式。

在這邊`h1`是一個選擇器

3.properties and values(屬性和變數)： 位於`{}`內，屬性和變數裝飾指定的選擇器。

在例圖中，
     
  
